import { ListaRecetas } from '@/components/receta/ListaRecetas';

/** Recetas emitidas por quien está en sesión y su estado en Farmacia. */
export default function RecetasScreen() {
  return <ListaRecetas />;
}
